docker-compose up -d
version: '3.8'
services:
  zubo:
    image: kakaxi088/zubo:v3.2.0
    container_name: zubo-app
    ports:
      - "8080:80"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
mkdir zubo-docker
cd zubo-docker
    
